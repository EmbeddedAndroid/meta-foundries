# Kernel config fragment for IMA (Integrity Measurement Architecture).
#
# Opt-in: pairs with userspace ima-evm-utils and a keyring policy.
# Enable per-factory via FIO_KERNEL_FEATURES.

CONFIG_IMA=y
CONFIG_IMA_APPRAISE=y
CONFIG_IMA_TRUSTED_KEYRING=y
CONFIG_IMA_DEFAULT_HASH_SHA256=y
