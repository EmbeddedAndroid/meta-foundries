# yaml-language-server: $schema=https://raw.githubusercontent.com/siemens/kas/master/kas/schema-kas.json
#
# Composable brick: meta-foundries on qemuarm64. Stock oe-core /
# poky distro, no vendor BSP. Pure portability smoke test: proves
# the layer parses and product packages assemble against vanilla
# upstream.

header:
  version: 14
  includes:
    - kas/ci/base.yml

distro: poky

defaults:
  repos:
    branch: master

repos:
  oe-core:
    url: https://github.com/openembedded/openembedded-core
    layers:
      meta:

  bitbake:
    url: https://github.com/openembedded/bitbake
    layers:
      .: excluded

  meta-yocto:
    url: https://git.yoctoproject.org/meta-yocto
    layers:
      meta-poky:

  meta-openembedded:
    url: https://git.openembedded.org/meta-openembedded
    layers:
      meta-oe:
      meta-python:
      meta-networking:
      meta-filesystems:

  meta-virtualization:
    url: https://git.yoctoproject.org/meta-virtualization

machine: qemuarm64

local_conf_header:
  base: |
    CONF_VERSION = "2"
    INIT_MANAGER = "systemd"
    PACKAGE_CLASSES = "package_ipk"
    INHERIT += "rm_work"
  sota: |
    DISTRO_FEATURES:append = " sota"
    INHERIT += "sota"
    SOTA_CLIENT = "aktualizr-lite"
  network: |
    # fioconfig RDEPENDS on networkmanager-nmcli; poky's default
    # provider for the network-configuration virtual is connman,
    # which conflicts at opkg-install time. Pick NetworkManager.
    DISTRO_FEATURES:append = " networkmanager"
    PREFERRED_RPROVIDER_network-configuration = "networkmanager"
    PREFERRED_RPROVIDER_virtual/network-configuration = "networkmanager"
